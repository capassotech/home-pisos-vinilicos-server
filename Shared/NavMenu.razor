<div class="top-row navbar" style="background: #2c3e50">
    <div class="flex-column justify-content-center align-content-center">
        <a class="navbar-brand" href=""><img src="/css/images/logo.png" alt="Logo de la Empresa"/></a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <i class="fa-solid fa-bars"></i> 
        </button>
    </div>
</div>

<div class="@NavMenuCssClass sidebar" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="productos">
                <i class="fa-solid fa-list-check"></i> Productos
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="categorias">
                <i class="fa-solid fa-table-cells-large"></i> Categorías
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="preguntas-frecuentes">
                <i class="fa-regular fa-circle-question"></i> Preguntas Frecuentes
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="contacto">
                <i class="fa-regular fa-address-card"></i> Contacto y RRSS
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}